name: Sync to repo1

on:
  push:
    branches:
      - main

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo2
      uses: actions/checkout@v2
      with:
        ref: main

    - name: Set up Git
      run: |
        git config --global user.name 'Hitesh'
        git config --global user.email 'hitesh@gmail.com'

    - name : "okok"
      run : |
          echo ${{ github.repository }}
          echo "above is repo nameeee"

